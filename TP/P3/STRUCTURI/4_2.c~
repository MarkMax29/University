/*Aplicația 4.2: Cu ajutorul structurii de la problema mai sus să se salveze într-un vector de structuri declarat static
informații despre mai multe fișiere (maxim 10). Să se citească de la tastatură informațiile despre fișiere și să se
salveze în fișierul memorie.txt.
*/

#include<stdio.h>
#include<stdlib.h>

typedef struct FISIER{

  unsigned int dim:10;
  unsigned int tip:2;
  unsigned int tfis:1;
  char nume[16];
}FISIER;

int main()
{
  int dim,tip,tfis,i=0;
  FISIER *v=NULL;
  v=(FISIER *)malloc(sizeof(FISIER));
  if(v==NULL)
    {
      perror(NULL);
      exit(-1);
    }
  
  while((scanf("%s %d %d %d",v[i].nume,&dim,&tip,&tfis))==4)
 
  printf("Introdu date despre fisier: ");scanf("%s %d %d %d",x.nume,&dim,&tip,&tfis);
  x.dim=dim;
  x.tip=tip;
  x.tfis=tfis;
  printf("nume %s - dimensiune %d - tip %d - tip fisier %d\n",x.nume,x.dim,x.tip,x.tfis);
  
  return 0;
}
