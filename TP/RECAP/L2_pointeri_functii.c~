#include <stdio.h>
#include <stdlib.h>
int compar(const void *elem1,const void *elem2)
{
  const float *e1=(const float*)elem1;
  const float *e2=(const float*)elem2;
  if(e1>e2)
    return 1;
  if(e1<e2)
    return -1;
  return 0;

}
void pb2_3()
{
  scanf("%d",&n);
  for(int i=0;i<n;i++)
    scanf("%f",v[i]);
  qsort(v,n,sizeof(float),(*compar)(const void,const void));
  
}
int main()
{
  int n;
  scanf("%d",&n);
  
  return 0;
}
